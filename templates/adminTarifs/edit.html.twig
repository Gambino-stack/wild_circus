{% extends 'layoutAdmin.html.twig' %}

{% block title %}Modifier les Tarifs{% endblock %}

{% block body %}
    <h1 class="text-primary text-center">Les Tarifs</h1>
    <div class="container">
        {{ include('adminTarifs/_form.html.twig', {'button_label': 'Modifier'}) }}

        <div class="row mx-0 d-flex justify-content-center">
            {{ include('adminTarifs/_delete_form.html.twig') }}</div>
        <div class="d-flex justify-content-start">
            <a class="btn btn-primary" href="{{ path('tarifs_index') }}">Retour</a></div>

    </div>

{% endblock %}
