{% extends 'base.html.twig' %}

{% block title %}Renouvelle ton mot de passe{% endblock %}

{% block body %}
    <h1 class="text-center text-primary mb-5">Renouvelle ton mot de passe</h1>
    <div class="container mb-5">
        {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">Reset password</button>
        {{ form_end(resetForm) }}
    </div>
{% endblock %}
